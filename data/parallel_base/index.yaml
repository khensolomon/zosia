# ./data/parallel_base/index.yaml
# Index of data files in this directory.

name: "parallel_base"
description: "Core parallel corpus for Zolai-English."
type: "parallel"

# Defines the language pairs for the training set by their base name.
# The script will automatically look for .zo and .en extensions.
train_pairs:
  - phrase
  # - word
  # - bsb-tedim

# Defines the language pairs for the test set.
test_pairs:
  - test
