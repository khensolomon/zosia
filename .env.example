# This is an example .env file.
# Create a copy named .env in the project root for local overrides.
# .env is ignored by git and should not be committed.

# --- Example Overrides ---

# To change the experiments directory for local runs:
# data.paths.experiments_dir = "./experiments/local_dev"

# To test with a smaller batch size:
# model.training.batch_size = 8

